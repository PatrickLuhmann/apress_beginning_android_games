Sample code for each chapter is in its own directory.
A directory suffix of "_as" means that the code has been configured as a project in Android Studio.
If a sample is ever configured for use in Eclipse, use a suffix like "_ec".
